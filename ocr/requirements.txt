# 本地 PaddleOCR 服务依赖
# Python 支持 3.8–3.12（paddlepaddle 2.6 上限为 3.12，尚不支持 3.13）；推荐用 3.12。
# start-ocr.sh 会自动优先选择 3.12 -> 3.11 -> 3.10。
#
# 说明：PaddleOCR 3.x 改了 API（predict/结果结构），本服务代码对 2.x/3.x 都做了兼容解析；
# 为稳妥默认锁在 2.9.x（与规则抽取匹配、经充分验证的一线）。如需升到 3.x，改 <3 上限即可，
# ocr_service.py 已尽量兼容，但请在机器上实测。
paddlepaddle>=2.6,<3
paddleocr>=3.7.0,<4
numpy<2
opencv-python-headless>=4.9
fastapi>=0.141.1
uvicorn[standard]>=0.52.4
